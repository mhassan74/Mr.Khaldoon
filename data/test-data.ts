/**
 * Non-secret, structured fixtures only — credentials stay in .env, never
 * here. Real subject/lesson names confirmed by walking through the live
 * app (2026-09-08) — content is Arabic even though the app chrome is
 * English.
 */
export const testData = {
  startLearning: {
    subjectName: 'Arabic',
    lessonName: 'إرادة التغيير',
  },
};
